# CLAUDE.md - 族谱软件 (Genealogy)

## 项目概述

这是一个基于 **Tauri 2.0 + React 19 + TypeScript** 构建的跨平台桌面族谱软件，用于管理家族谱系信息。

该项目是一个"本地服务端 + 多客户端"架构——数据私密性高（用户不想把家族信息上传到云端），但又需要多设备访问

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户电脑（服务端核心）                      │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                  Tauri Desktop App                     │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐  │  │
│  │  │  Rust 后端   │  │  SQLite     │  │  本地 Web    │  │
│  │  │  HTTP API   │◄─┤  (族谱数据)  │  │  前端界面     │  │
│  │  │  端口: 8080  │  │  + 媒体文件  │  │  (React)     │  │
│  │  └──────┬──────┘  └─────────────┘  └──────────────┘  │  │
│  │         │                                            │  │
│  │  ┌──────▼──────┐  系统托盘后台运行                     │  │
│  │  │  WebView    │  开机自启                            │  │
│  │  │  (加载      │  数据自动备份                          │  │
│  │  │  localhost) │                                      │  │
│  │  └─────────────┘                                      │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP/WebSocket (局域网)
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│   iOS App    │   │  Android App │   │   浏览器     │
│  (Tauri v2   │   │  (Tauri v2   │   │  (任何设备)  │
│   或 Swift)  │   │   或 Flutter)│   │              │
└──────────────┘   └──────────────┘   └──────────────┘
```

### 功能特性

- 跨平台支持 (Windows, macOS, Linux, iOS, Android)
- 家族谱系可视化展示
- 家谱树生成与导航
- 个人信息管理 (姓名、出生日期、死亡日期、出生地、工作等)
- 本地存储与同步 (SQLite 数据库)
- 家庭网运行程序，可以支持纯离线模式，也支持允许公网访问(通过frpc 进行内网穿透)

#### 族谱软件功能点描述

1. 支持创建家族，但是该功能通过配置来控制，默认不支持创建家族，只有通过配置开启才可以创建家族。如果没有开启，用看到的是当前我的家族
2. 关系标签管理
   - 支持用户自定义关系标签
     - 配偶关系：离异、续弦、再婚、妻子、二夫人、妾女、招夫养子 等
     - 父母子女关系：收养、亲生、嫡出、庶出 等
     - 兄弟姐妹关系：长子、 次子、长女、此女、三女、幺女 等
     - 特殊标签：革命烈士、英雄烈士、烈士、抗日老兵 等
   - 每个关系标签都可以设置不同的颜色，每个关系标签都可以设置不同的颜色
   - 可以在后续创建用户信息时来创建标签
3. 家族成员个人信息管理
   - 人信息字段，例如：姓名、性别、代系、突出事迹，出生日期、死亡日期、出生地、工作, 照片等
   - 家族成员个人 关联的丈夫，妻子，子辈，父辈等
     - 配偶关系：离异、续弦、再婚、妻子、二夫人、妾女、招夫养子 等
     - 父母子女关系：收养、亲生、嫡出、庶出 等
     - 兄弟姐妹关系：长子、 次子、长女、此女、三女、幺女 等
     - 特殊标签：革命烈士、英雄烈士、烈士、抗日老兵 等
4. 平台用户管理
   - 该用户管理跟家族成员个人信息管理是分开的，用户管理主要是用来管理用户账号，包括用户登录、注册、权限管理等。
   - 只有管理员才有权限添加、删除、修改用户账号，普通用户只能查看自己的账号信息。
   - 管理员可以把平台用户管理关联到家族成员个人信息，关联后，平台用户只能修改自己关联的家族成员个人信息。
   - 管理员有权限添加、删除、修改家族成员个人信息
   - 普通用户只能查看、编辑自己的个人信息
5. 安装软件时，需要用户提供数据存储的路径，因为图片等数据需要存储在本地
6. 支持微信扫描登陆
7. 支持扫描付费后开启内网穿透功能

## 界面设计

先根据上面的功能点描述，设计出系统的界面。如果不行我这边再设计。

### UI 规范要求

项目根目录新增了 `UI_SPEC.md`，作为本项目正式的 UI / UX 规范文档。

从现在开始，所有前端页面、组件、弹窗、表单、表格、树图、导航、状态反馈、空状态、错误状态、权限状态的设计与实现，**必须符合 `UI_SPEC.md` 中定义的 UI 规范**。

执行要求如下：

- 所有新增 UI 设计与前端实现，必须以 `UI_SPEC.md` 为最高优先级规范
- 现有页面后续重构与优化，也必须逐步向 `UI_SPEC.md` 对齐
- 不允许绕过基础组件体系随意定义视觉样式
- 不允许脱离项目技术栈单独引入不受控的 UI 风格
- React + TypeScript + Tailwind CSS + Radix UI 的实现方式，必须符合 `UI_SPEC.md` 中约定的最佳实践

## 技术栈

| 层次         | 技术                |
| ------------ | ------------------- |
| 前端框架     | React 19.1.0        |
| UI 语言      | TypeScript 5.8.3    |
| 构建工具     | Vite 7.0.4          |
| 桌面框架     | Tauri 2.x           |
| 后端语言     | Rust                |
| 包管理器     | pnpm 10.11.0        |
| 数据库       | SQLite 3            |
| 可视化图表库 | react-d3-tree 3.6.6 |

## 项目结构

```
genealogy/
├── src/                          # React 前端源码
│   ├── main.tsx                  # React 入口
│   ├── App.tsx                   # 主应用组件
│   └── App.css                   # 主样式
│
├── src-tauri/                    # Tauri/Rust 后端
│   ├── src/
│   │   ├── lib.rs                # Rust 库入口
│   │   └── main.rs               # Rust 程序入口
│   ├── Cargo.toml                # Rust 依赖
│   └── tauri.conf.json           # Tauri 配置
│
├── package.json                  # NPM 依赖
├── vite.config.ts                # Vite 配置
└── tsconfig.json                 # TypeScript 配置
```

## 开发命令

| 命令               | 作用                        |
| ------------------ | --------------------------- |
| `pnpm dev`         | 启动前端开发服务器          |
| `pnpm tauri dev`   | 启动 Tauri 桌面应用开发模式 |
| `pnpm build`       | 构建前端                    |
| `pnpm tauri build` | 构建完整 Tauri 应用         |

## 关键文件

- `src-tauri/src/lib.rs` - Rust 后端命令入口
- `src/App.tsx` - React 主组件
- `src-tauri/tauri.conf.json` - 桌面窗口配置

## 架构说明

```
┌─────────────────────────────┐
│    Tauri Desktop App        │
├─────────────────────────────┤
│  Frontend (React)           │◄──►  Backend (Rust)
│  - Components               │     - Commands
│  - State Management         │     - Data Processing
│  - UI/UX                    │     - File I/O
└─────────────────────────────┘
```

## 待补充

- [ ] 族谱数据模型设计
- [ ] 核心功能模块规划
- [ ] 组件结构设计
- [ ] 状态管理方案
- [ ] 数据存储方案

---

**最后更新**: 2026-04-10
