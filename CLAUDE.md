# CLAUDE.md - 族谱软件 (Genealogy)

## 项目概述

这是一个基于 **Tauri 2.0 + React 19 + TypeScript** 构建的跨平台桌面族谱软件，用于管理家族谱系信息。

该项目是一个"本地服务端 + 多客户端"架构——数据私密性高（用户不想把家族信息上传到云端），但又需要多设备访问

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户电脑（服务端核心）                      │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                  Tauri Desktop App                     │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐  │  │
│  │  │  Rust 后端   │  │  SQLite     │  │  本地 Web    │  │
│  │  │  HTTP API   │◄─┤  (族谱数据)  │  │  前端界面     │  │
│  │  │  端口: 8080  │  │  + 媒体文件  │  │  (React)     │  │
│  │  └──────┬──────┘  └─────────────┘  └───────┬──────┘  │  │
│  │         │                                    │          │  │
│  │         │    ┌──────────────────────────────┴────┐    │  │
│  │         │    │  HTTP Server (axum)                │    │  │
│  │         │    │  - 端口 8080                        │    │  │
│  │         │    │  - 同时提供 API 和静态文件服务        │    │  │
│  │         │    └─────────────────────────────────────┘    │  │
│  │         │                                            │  │
│  │  ┌──────▼──────┐  系统托盘后台运行                     │  │
│  │  │  WebView    │  开机自启                            │  │
│  │  │  (加载      │  数据自动备份                          │  │
│  │  │  localhost) │                                      │  │
│  │  └─────────────┘                                      │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP (局域网/公网)
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│   iOS App    │   │  Android App │   │   浏览器     │
│  (Tauri v2   │   │  (Tauri v2   │   │  (任何设备)  │
│   或 Swift)  │   │   或 Flutter)│   │  http://IP:8080│
└──────────────┘   └──────────────┘   └──────────────┘
```

### 功能特性

- 跨平台支持 (Windows, macOS, Linux, iOS, Android)
- 家族谱系可视化展示
- 家谱树生成与导航
- 个人信息管理 (姓名、出生日期、死亡日期、出生地、工作等)
- 本地存储与同步 (SQLite 数据库)
- **局域网浏览器访问**: 桌面端启动 HTTP 服务器 (端口 8080)，提供 API 和前端页面
- 桌面端与浏览器端差异化登录/注册体验

#### 族谱软件功能点描述
1. 支持创建家族，但是该功能通过配置来控制，默认不支持创建家族，只有通过配置开启才可以创建家族。如果没有开启，用看到的是当前我的家族
2. 关系标签管理
    - 支持用户自定义关系标签
        - 配偶关系：离异、续弦、再婚、妻子、二夫人、妾女、招夫养子 等
        - 父母子女关系：收养、亲生、嫡出、庶出 等
        - 兄弟姐妹关系：长子、 次子、长女、此女、三女、幺女 等
        - 特殊标签：革命烈士、英雄烈士、烈士、抗日老兵 等
    - 每个关系标签都可以设置不同的颜色，每个关系标签都可以设置不同的颜色
    - 可以在后续创建用户信息时来创建标签
3. 家族成员个人信息管理
    - 成员信息字段：
        - **姓名**：必填
        - **性别**：必填
        - **代数 (generation)**：程序根据父/母子关系自动计算，用户不可填写或修改
        - **字辈 (generation_word)**：非必填，名字中的辈分用字
        - 出生日期、死亡日期、是否离世、出生地、工作、照片等
        - 突出事迹、生平简介
    - 家族成员个人 关联的丈夫，妻子，子辈，父辈等
        - 配偶关系：离异、续弦、再婚、妻子、二夫人、妾女、招夫养子 等
        - 父母子女关系：收养、亲生、嫡出、庶出 等
        - 兄弟姐妹关系：长子、 次子、长女、此女、三女、幺女 等
        - 特殊标签：革命烈士、英雄烈士、烈士、抗日老兵 等
4. 平台用户管理
    - 该用户管理跟家族成员个人信息管理是分开的，用户管理主要是用来管理用户账号，包括用户登录、注册、权限管理等。
    - 只有管理员才有权限添加、删除、修改用户账号，普通用户只能查看自己的账号信息。
    - 管理员可以把平台用户管理关联到家族成员个人信息，关联后，平台用户只能修改自己关联的家族成员个人信息。
    - 管理员有权限添加、删除、修改家族成员个人信息
    - 普通用户只能查看、编辑自己的个人信息
5. 安装软件时，需要用户提供数据存储的路径，因为图片等数据需要存储在本地
6. 支持微信扫描登陆
7. 支持扫描付费后开启内网穿透功能


## 界面设计
先根据上面的功能点描述，设计出系统的界面。如果不行我这边再设计。


## 关键概念说明

### 代数 vs 字辈（辈分用字）

这是两个完全不同且独立的概念，**严禁混淆**：

| 字段 | 说明 | 来源 | 性质 |
|------|------|------|------|
| **代数 (generation)** | 表示成员在家族中的代数/辈分，从1开始递增 | 根据父子关系自动计算（根成员为第1代，其子女为第2代，以此类推） | **程序计算，用户不可修改** |
| **字辈 (generation_word)** | 名字中使用的辈分用字，如"仁、义、礼、智、信" | 家族传统，由家族字辈谱决定 | **非必填**，由用户手动指定 |

**代数计算逻辑**：
- 根成员（没有父亲的成员）代数 = 1
- 其他成员的代数 = 父亲的代数 + 1
- 通过递归遍历父子关系链计算
- **代数字段由程序自动计算和维护，用户无法直接修改**

**字辈使用场景**：
- 用户配置家族字辈序列（如"仁,义,礼,智,信"）
- 在成员姓名中体现辈分（如"孔仁xxx"中的"仁"）
- 显示代数标签时使用字辈（如第5代对应"礼"字，显示"礼"）

### 关系标签类型

- **配偶关系**：离异、续弦、再婚、妻子、二夫人、妾女、招夫养子 等
- **父母子女关系**：收养、亲生、嫡出、庶出 等
- **兄弟姐妹关系**：长子、次子、长女、此女、三女、幺女 等
- **特殊标签**：革命烈士、英雄烈士、烈士、抗日老兵 等

---

## 技术栈

| 层次 | 技术 |
|------|------|
| 前端框架 | React 19.1.0 |
| UI 语言 | TypeScript 5.8.3 |
| 构建工具 | Vite 7.0.4 |
| 桌面框架 | Tauri 2.x |
| 后端语言 | Rust |
| HTTP 服务 | Axum 0.7 |
| 包管理器 | pnpm 10.11.0 |
| 数据库 | SQLite 3 |
| 可视化图表库 | react-d3-tree 3.6.6 |

## 项目结构

```
genealogy/
├── src/                          # React 前端源码
│   ├── main.tsx                  # React 入口
│   ├── App.tsx                   # 主应用组件
│   ├── pages/
│   │   ├── LoginPage.tsx         # 登录/注册页面
│   │   ├── HomePage.tsx          # 首页
│   │   ├── TreePage.tsx          # 族谱树页面
│   │   └── UsersPage.tsx         # 用户管理页面
│   ├── components/
│   │   ├── Layout.tsx            # 主布局组件
│   │   └── ui/                   # UI 组件库
│   ├── api/
│   │   └── client.ts             # API 客户端
│   └── stores/
│       └── index.ts              # 状态管理
│
├── src-tauri/                    # Tauri/Rust 后端
│   ├── src/
│   │   ├── lib.rs                # Rust 库入口 (启动逻辑)
│   │   ├── api/                  # HTTP API
│   │   │   ├── mod.rs
│   │   │   ├── router.rs         # 路由配置
│   │   │   └── handlers/         # API 处理器
│   │   │       ├── mod.rs
│   │   │       ├── auth.rs       # 认证 API
│   │   │       ├── members.rs    # 成员 API
│   │   │       └── ...
│   │   ├── auth.rs               # JWT 认证
│   │   ├── db.rs                 # 数据库初始化
│   │   └── models.rs             # 数据模型
│   ├── Cargo.toml                # Rust 依赖
│   └── tauri.conf.json           # Tauri 配置
│
├── package.json                  # NPM 依赖
├── vite.config.ts                # Vite 配置
└── tsconfig.json                 # TypeScript 配置
```

## 开发命令

| 命令 | 作用 |
|------|------|
| `pnpm dev` | 启动前端开发服务器 |
| `pnpm tauri dev` | 启动 Tauri 桌面应用开发模式 |
| `pnpm build` | 构建前端 |
| `pnpm tauri build` | 构建完整 Tauri 应用 |

## 关键文件

- `src-tauri/src/lib.rs` - Rust 后端启动入口，HTTP 服务器初始化
- `src-tauri/src/api/router.rs` - Axum 路由配置，API + 静态文件服务
- `src-tauri/tauri.conf.json` - 桌面窗口配置（User-Agent 等）
- `src/pages/LoginPage.tsx` - 登录/注册页面，区分桌面端和浏览器端

## 已实现功能

### 1. 浏览器访问支持

桌面端启动 HTTP 服务器 (端口 8080)，同时提供：
- **API 服务**: `/api/*` 路由
- **静态文件服务**: 前端页面 (SPA)

```rust
// src-tauri/src/lib.rs
// HTTP 服务器在 .setup() 中启动，确保窗口先显示
std::thread::spawn(move || {
    let app = api::create_router(http_db, Some(dist_path));
    let listener = tokio::net::TcpListener::bind("0.0.0.0:8080").await.unwrap();
    axum::serve(listener, app).await.unwrap();
});
```

### 2. 桌面端与浏览器端区分

通过自定义 User-Agent `GenealogyDesktop/1.0` 区分客户端类型：

```json
// tauri.conf.json
{
  "app": {
    "windows": [{
      "userAgent": "GenealogyDesktop/1.0"
    }]
  }
}
```

前端检测逻辑：
```tsx
// src/pages/LoginPage.tsx
const isDesktop = navigator.userAgent.includes('GenealogyDesktop')
```

### 3. 差异化登录/注册体验

| 客户端 | 行为 |
|--------|------|
| **桌面端** | 显示登录页面，提供"首次使用？创建管理员账号"切换按钮 |
| **浏览器端** | 仅显示登录页面，提示"联系管理员创建账号" |

### 4. 数据库路径

使用 Tauri 的 `app_data_dir()` 获取应用数据目录，确保数据库路径正确：

```rust
let app_data_dir = app.path().app_data_dir().expect("Failed to get app data dir");
let db_path = app_data_dir.join(".genealogy.db");
```

### 5. 前端静态文件打包

`dist` 文件夹通过 `resources` 配置打包到 app bundle：

```json
// tauri.conf.json
{
  "bundle": {
    "resources": {
      "../dist": "dist"
    }
  }
}
```

### 6. 注册后自动登录

注册 API 成功后自动生成 token 并返回用户信息：

```rust
// src-tauri/src/api/handlers/auth.rs
match create_token(id, &req.username, &role) {
    Ok(token) => {
        let response = LoginResponse { token, user };
        (StatusCode::CREATED, Json(json!({ "data": response })))
    }
    Err(e) => (StatusCode::INTERNAL_SERVER_ERROR, ...),
}
```

## API 路由

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/health` | 健康检查 | 否 |
| POST | `/api/auth/register` | 注册账号 | 否 |
| POST | `/api/auth/login` | 登录 | 否 |
| GET | `/api/users/me` | 获取当前用户 | 是 |
| GET | `/api/admin/users` | 获取所有用户 | 管理员 |
| GET/POST | `/api/members` | 成员列表/创建 | - |
| GET/PUT/DELETE | `/api/members/:id` | 成员详情/更新/删除 | - |
| GET/POST | `/api/member-relations` | 关系列表/创建 | - |
| GET/POST | `/api/relation-tags` | 标签列表/创建 | - |

---

**最后更新**: 2026-04-11
