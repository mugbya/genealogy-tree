# UI_SPEC.md - 族谱软件 UI 规范

## 1. 文档目的

本文档定义 `genealogy-tree` 项目的统一 UI / UX 规范，适用于所有前端界面设计、组件开发、页面实现与后续迭代。

本规范服务于以下目标：

- 保证整个产品在视觉、交互、信息结构上的一致性
- 提升桌面端与多端访问场景下的可用性与可维护性
- 为 React + TypeScript + Tailwind CSS + Radix UI 技术栈提供明确落地标准
- 为后续页面设计、组件封装、设计评审、代码评审提供统一依据

---

## 2. 强制性要求

### 2.1 必须遵守

从本规范生效起，所有新增页面、组件、弹窗、表单、表格、树图、导航、状态反馈、空状态、错误状态、权限状态，**必须符合本 UI 规范**。

### 2.2 优先级

当出现规范冲突时，优先级如下：

1. 本文档 `UI_SPEC.md`
2. 根目录 `CLAUDE.md`
3. 现有代码实现
4. 临时个人习惯

### 2.3 禁止事项

禁止以下行为：

- 未经设计约束，随意新增颜色、圆角、阴影、间距体系
- 页面之间使用不同的表单布局逻辑
- 同类操作使用不同按钮语义
- 同类状态使用不同颜色表达
- 在业务页面中直接堆砌大量匿名 Tailwind 类而不抽象组件
- 绕过基础组件体系，重复造轮子
- 为了“看起来炫”而牺牲信息密度、可读性、可操作性
- 在族谱树、成员详情、关系编辑等核心场景中使用不稳定或难理解的交互

---

## 3. 产品 UI / UX 总原则

族谱软件不是营销型产品，而是**高信息密度、长期使用、强调准确性与可追溯性**的工具型产品。因此 UI 设计必须遵循以下原则：

### 3.1 信息优先

界面首先服务于“看清楚、找得到、改得准”，而不是装饰性视觉表达。

### 3.2 稳定优先

用户管理的是家族数据、关系数据、历史信息，界面必须稳定、克制、可预测。

### 3.3 层级清晰

页面必须明确区分：

- 全局导航
- 模块导航
- 页面标题区
- 操作区
- 内容区
- 详情区
- 辅助信息区

### 3.4 操作可回溯

涉及新增、编辑、删除、关系变更、标签变更、权限变更的操作，必须提供明确反馈，必要时提供二次确认。

### 3.5 适合长时间使用

视觉风格应克制、耐看、低疲劳，避免高饱和大面积色块、过度动画、过强对比。

### 3.6 面向真实家谱场景

设计必须考虑：

- 家族成员数量多
- 关系复杂
- 标签多样
- 数据字段长
- 图片与文本并存
- 管理员与普通用户权限不同
- 桌面端是核心使用场景

---

## 4. 设计风格定位

### 4.1 风格关键词

- 专业
- 稳定
- 清晰
- 现代
- 克制
- 高可读
- 高信息密度
- 轻量东方文化气质

### 4.2 风格说明

整体风格采用**现代管理系统 + 家谱文化产品**的融合方向：

- 基础框架采用现代后台式布局
- 视觉语言保持简洁、留白适中、边界清晰
- 在局部图标、标题、标签、树图节点中体现家谱产品特征
- 不使用拟物化、重装饰、强渐变、大面积玻璃拟态作为主风格

### 4.3 视觉基调

- 主背景：浅色、中性、低干扰
- 内容容器：白色卡片或浅层级面板
- 主色：稳重的靛蓝 / 蓝紫系
- 辅助色：中性灰体系
- 状态色：成功 / 警告 / 错误 / 信息明确区分
- 强调方式：优先使用层级、边框、字重、间距，其次才是颜色

---

## 5. 技术栈落地规范

本项目 UI 必须与以下技术栈保持一致：

- React 19
- TypeScript
- Tailwind CSS 4
- Radix UI
- class-variance-authority
- tailwind-merge
- lucide-react
- react-d3-tree

### 5.1 组件实现原则

- 基础交互组件优先基于 `Radix UI` 封装
- 组件变体统一使用 `class-variance-authority`
- 类名合并统一使用 `tailwind-merge` + `clsx`
- 图标统一使用 `lucide-react`
- 树图可视化统一围绕 `react-d3-tree` 做二次封装
- 页面不得直接依赖第三方组件默认样式，必须纳入本规范体系

### 5.2 样式组织原则

推荐分层如下：

- `tokens`：颜色、圆角、阴影、间距、字号、层级
- `base`：全局基础样式
- `ui`：基础组件
- `patterns`：复合模式，如搜索栏、筛选栏、详情头部、统计卡片
- `pages`：页面级布局，不承载基础视觉规则

### 5.3 类型安全要求

所有 UI 组件必须：

- 有明确的 Props 类型
- 变体可枚举
- 状态可枚举
- 避免使用 `any`
- 避免隐式布尔组合导致样式不可控

---

## 6. 设计 Token 规范

以下为推荐标准，后续代码实现必须尽量与之对齐。

---

## 7. 颜色系统

### 7.1 颜色角色

颜色必须按“角色”使用，而不是按“喜好”使用。

#### 核心角色

- `primary`：主操作、主强调、主品牌识别
- `secondary`：次级容器、次级按钮、弱强调
- `accent`：悬停、选中背景、辅助强调
- `muted`：弱化信息、说明文字、占位内容
- `background`：页面背景
- `foreground`：主文字
- `card`：卡片背景
- `border`：边框
- `input`：输入框边框 / 背景边界
- `ring`：焦点态
- `destructive`：危险操作
- `success`：成功状态
- `warning`：警告状态
- `info`：信息状态

### 7.2 推荐基础色值

以下为推荐默认值，可作为项目标准主题：

- `primary`: `#4F46E5`
- `primary-foreground`: `#FFFFFF`

- `secondary`: `#F4F4F5`
- `secondary-foreground`: `#18181B`

- `accent`: `#F4F4F5`
- `accent-foreground`: `#18181B`

- `muted`: `#F4F4F5`
- `muted-foreground`: `#71717A`

- `background`: `#FAFAFA`
- `foreground`: `#18181B`

- `card`: `#FFFFFF`
- `card-foreground`: `#18181B`

- `border`: `#E4E4E7`
- `input`: `#E4E4E7`
- `ring`: `#4F46E5`

- `destructive`: `#DC2626`
- `destructive-foreground`: `#FFFFFF`

- `success`: `#16A34A`
- `success-foreground`: `#FFFFFF`

- `warning`: `#D97706`
- `warning-foreground`: `#FFFFFF`

- `info`: `#2563EB`
- `info-foreground`: `#FFFFFF`

### 7.3 中性色阶建议

建议建立中性色阶，至少包含：

- `zinc-50`
- `zinc-100`
- `zinc-200`
- `zinc-300`
- `zinc-400`
- `zinc-500`
- `zinc-600`
- `zinc-700`
- `zinc-800`
- `zinc-900`

用途建议：

- 页面背景：`zinc-50`
- 分割线 / 边框：`zinc-200`
- 次级文字：`zinc-500` / `zinc-600`
- 主文字：`zinc-900`

### 7.4 颜色使用规则

- 一个页面主强调色不超过 1 个
- 状态色只用于状态，不用于装饰
- 危险色只用于删除、不可逆操作、严重错误
- 成功色只用于成功反馈、正常状态标识
- 标签颜色允许扩展，但必须保证文字对比度与可读性
- 不允许在同一页面中出现多个无语义的彩色按钮

---

## 8. 字体与排版规范

### 8.1 字体族

统一使用：

- `Inter`
- 系统回退：`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`

中文显示优先保证清晰、稳定、系统兼容。

### 8.2 字重

推荐使用：

- `400`：正文
- `500`：次强调、标签、按钮
- `600`：区块标题、卡片标题
- `700`：页面主标题、关键数字

### 8.3 字号体系

推荐标准：

- `12px`：辅助说明、表格次信息、标签小字
- `13px`：紧凑型表格内容
- `14px`：默认正文、表单、按钮、列表
- `16px`：重点正文、小标题
- `18px`：区块标题
- `20px`：页面标题
- `24px`：大页面标题
- `30px+`：仅用于首页概览或极少数重点数据

### 8.4 行高

推荐：

- 小字：`1.4`
- 正文：`1.5`
- 标题：`1.2 ~ 1.35`

### 8.5 文本规则

- 页面标题必须简洁明确
- 按钮文案必须是动词导向，如“新增成员”“保存修改”
- 表单标签必须使用业务语言，不使用开发内部命名
- 错误提示必须可理解，不允许只显示技术错误码
- 长文本必须支持换行或折叠，不允许溢出破坏布局

---

## 9. 间距系统

### 9.1 基础单位

采用 `4px` 作为最小间距单位。

### 9.2 推荐间距刻度

- `4`
- `8`
- `12`
- `16`
- `20`
- `24`
- `32`
- `40`
- `48`
- `64`

### 9.3 使用建议

- 图标与文字：`8px`
- 表单项内部：`8px ~ 12px`
- 卡片内边距：`16px ~ 24px`
- 区块之间：`24px ~ 32px`
- 页面主区块之间：`32px ~ 40px`

### 9.4 一致性要求

同一页面中，相同层级的区块必须使用相同间距，不允许“凭感觉”调整。

---

## 10. 圆角、边框、阴影

### 10.1 圆角体系

推荐：

- `sm`: `6px`
- `md`: `8px`
- `lg`: `12px`
- `xl`: `16px`
- `full`: `9999px`

使用建议：

- 输入框 / 按钮：`8px`
- 卡片：`12px`
- 弹窗：`16px`
- 标签 / 胶囊：`9999px`

### 10.2 边框

- 默认边框：`1px solid var(--border)`
- 强调边框：用于选中态、焦点态、拖拽态
- 分割线必须轻量，不可喧宾夺主

### 10.3 阴影

推荐仅使用 3 级：

- `shadow-sm`：轻微悬浮
- `shadow-md`：卡片 / 下拉 / 浮层
- `shadow-lg`：弹窗 / 重要浮层

禁止使用过重阴影造成廉价感。

---

## 11. 布局规范

### 11.1 总体布局模型

桌面端采用以下结构：

- 顶部全局导航栏
- 左侧模块导航或二级导航（按页面需要）
- 主内容区
- 右侧详情 / 辅助面板（仅在复杂页面中使用）

### 11.2 页面宽度

推荐：

- 主内容最大宽度：`1280px`
- 常规页面容器：`max-w-7xl`
- 表单详情页：`max-w-5xl`
- 阅读型页面：`max-w-4xl`

### 11.3 页面结构模板

每个页面应尽量包含以下结构：

1. 页面标题区
2. 页面说明 / 面包屑（可选）
3. 主操作区
4. 筛选 / 搜索区
5. 内容展示区
6. 分页 / 批量操作区
7. 辅助说明 / 状态反馈区

### 11.4 栅格建议

- 桌面端优先使用 12 栅格思维
- 表单详情页常用 `2-column`
- 列表 + 详情页常用 `7:5` 或 `8:4`
- 统计卡片常用 `4列` 或 `3列`

### 11.5 响应式原则

虽然桌面端优先，但仍需保证：

- 平板可用
- 窄屏不崩
- 关键操作不丢失
- 导航可折叠
- 表格可横向滚动
- 树图可缩放 / 拖拽 / 聚焦

---

## 12. 导航规范

### 12.1 全局导航

全局导航承载一级模块：

- 首页
- 族谱树
- 成员管理
- 关系管理
- 标签管理
- 用户管理
- 设置

### 12.2 导航要求

- 当前页面必须有明确激活态
- 激活态优先通过背景、字重、图标色体现
- 导航项文案必须简短
- 图标必须与业务语义一致
- 不允许同级导航超过 7 个核心项，超出需分组

### 12.3 面包屑

以下场景建议使用面包屑：

- 从列表进入详情
- 从树图进入成员详情
- 从设置进入子配置页
- 多层级管理页

### 12.4 标签页导航

标签页适用于同一对象下的并列信息切换，例如：

- 成员详情
  - 基本信息
  - 关系信息
  - 生平事迹
  - 媒体资料
  - 操作记录

---

## 13. 页面类型规范

### 13.1 首页 / 仪表盘

首页应展示：

- 当前家族概览
- 成员总数
- 代系分布
- 最近新增成员
- 最近编辑记录
- 待处理事项
- 快捷入口

首页不是装饰页，必须可操作、可导航。

### 13.2 列表页

适用于：

- 成员列表
- 用户列表
- 标签列表
- 关系记录列表

列表页必须包含：

- 页面标题
- 搜索
- 筛选
- 排序
- 批量操作（如适用）
- 表格 / 卡片列表
- 分页
- 空状态

### 13.3 详情页

适用于：

- 成员详情
- 用户详情
- 家族详情
- 标签详情

详情页必须强调：

- 关键信息优先
- 信息分组清晰
- 编辑入口明确
- 关系信息可视化
- 历史信息可追溯

### 13.4 编辑页 / 表单页

适用于：

- 新增成员
- 编辑成员
- 新增关系
- 编辑标签
- 设置数据路径

要求：

- 表单分组清晰
- 必填项明确
- 错误提示就近显示
- 保存 / 取消位置固定
- 离开未保存页面时必须提醒

### 13.5 树图页

树图页是核心页面，必须重点设计：

- 支持缩放、拖拽、居中、聚焦
- 支持节点选中
- 支持从节点快速查看详情
- 支持关系标签可视化
- 支持复杂关系下的清晰展示
- 支持切换视图密度
- 支持搜索定位成员
- 支持回到根节点 / 当前焦点节点

---

## 14. 组件规范

### 14.1 基础原则

所有基础组件必须具备：

- 默认态
- 悬停态
- 激活态
- 禁用态
- 焦点态
- 错误态（如适用）
- 加载态（如适用）

---

## 15. 按钮规范

### 15.1 按钮层级

必须统一为以下层级：

- `Primary`：页面主操作，每个区域通常只有一个
- `Secondary`：次级操作
- `Outline`：辅助操作
- `Ghost`：低干扰操作
- `Destructive`：危险操作
- `Link`：文本型跳转

### 15.2 使用规则

- 一个操作区最多一个主按钮
- 删除、解绑、清空必须使用危险按钮
- 取消通常使用 `Secondary` 或 `Ghost`
- 表格行内操作优先使用 `Ghost` / `Link`

### 15.3 尺寸

- `sm`
- `md`
- `lg`
- `icon`

默认使用 `md`。

### 15.4 文案规范

正确示例：

- 新增成员
- 保存修改
- 添加关系
- 删除标签
- 绑定账号

错误示例：

- 确定
- 提交
- 操作
- 点击这里

除非在标准确认弹窗中，否则避免模糊文案。

---

## 16. 输入框与表单规范

### 16.1 表单布局

推荐：

- 简单表单：单列
- 详情编辑：双列
- 长文本：独占一行
- 关系编辑：分组卡片式布局

### 16.2 表单字段类型

必须根据业务选择合适控件：

- 姓名：文本输入
- 性别：单选 / 下拉
- 出生日期：日期选择
- 死亡日期：日期选择
- 出生地：文本输入 + 可扩展结构化
- 工作：文本输入
- 突出事迹：多行文本
- 标签：多选 / 标签选择器
- 关系：结构化关系编辑器
- 照片：上传组件

### 16.3 标签与说明

- 每个字段必须有标签
- 必填项使用统一标识
- 复杂字段必须有帮助说明
- 错误提示显示在字段下方
- 不允许把错误提示只放在页面顶部

### 16.4 校验规则

- 实时校验与提交校验结合
- 错误文案必须明确指出问题
- 日期逻辑必须校验，例如出生日期不能晚于死亡日期
- 关系数据必须校验冲突与循环依赖风险

---

## 17. 表格规范

### 17.1 适用场景

适用于高密度结构化数据：

- 成员列表
- 用户列表
- 标签列表
- 操作记录

### 17.2 表格要求

- 表头固定语义清晰
- 支持排序的列必须有明确提示
- 行高统一
- 行 hover 可见
- 当前选中行可见
- 行内操作不超过 3 个主入口
- 超长文本必须截断并支持查看完整内容

### 17.3 推荐列内容

成员列表建议包含：

- 姓名
- 性别
- 代系
- 出生日期
- 状态
- 标签
- 最近更新时间
- 操作

### 17.4 空状态

表格无数据时必须显示：

- 原因说明
- 建议操作
- 主操作入口

---

## 18. 卡片规范

### 18.1 适用场景

适用于：

- 首页统计
- 成员摘要
- 关系摘要
- 快捷入口
- 空状态引导

### 18.2 卡片结构

推荐包含：

- 标题
- 辅助说明
- 主内容
- 操作区

### 18.3 卡片交互

- 可点击卡片必须有 hover 态
- 不可点击卡片不应伪装成可点击
- 卡片内多个操作必须有主次关系

---

## 19. 标签与徽标规范

### 19.1 标签用途

标签用于表达：

- 关系类型
- 身份属性
- 特殊荣誉
- 状态
- 权限
- 数据来源

### 19.2 标签颜色规则

标签颜色必须有语义或可配置来源，不允许随机。

建议：

- 配偶关系：紫 / 粉紫系
- 父母子女关系：蓝系
- 兄弟姐妹关系：青系
- 特殊荣誉：橙 / 红系
- 系统状态：按成功 / 警告 / 错误 / 信息色

### 19.3 标签可读性

- 文字必须清晰
- 背景色不能过深导致压迫感
- 同屏标签数量过多时必须支持折叠或分组

---

## 20. 弹窗、抽屉、浮层规范

### 20.1 使用原则

- 简单确认：弹窗
- 中等复杂编辑：弹窗或抽屉
- 高复杂编辑：独立页面
- 辅助说明：Popover / Tooltip

### 20.2 弹窗要求

- 必须有标题
- 必须有关闭方式
- 必须有明确主操作与取消操作
- 危险操作必须二次确认
- 不允许在小弹窗中塞入复杂长表单

### 20.3 抽屉要求

适用于：

- 快速查看成员详情
- 快速编辑少量字段
- 从树图节点侧边展开详情

---

## 21. 树图专项规范

### 21.1 树图定位

族谱树是本产品最核心的可视化模块，必须优先保证：

- 可读
- 可导航
- 可定位
- 可扩展
- 可解释

### 21.2 节点信息层级

节点建议展示：

- 姓名
- 性别
- 生卒年份
- 关键标签
- 当前选中状态

### 21.3 节点视觉规则

- 节点尺寸必须统一分级
- 当前焦点节点必须明显高亮
- 关系线必须清晰
- 不同关系类型可通过颜色 / 线型辅助区分，但不能过度复杂
- 节点内容过多时必须折叠摘要，不可全部堆满

### 21.4 树图交互

必须支持：

- 缩放
- 拖拽
- 节点点击
- 节点 hover
- 搜索定位
- 回到中心
- 聚焦当前成员
- 展开 / 收起分支

### 21.5 树图与详情联动

点击节点后，建议联动：

- 右侧详情面板
- 底部信息栏
- 弹出快捷操作菜单

### 21.6 性能原则

当成员数量较大时：

- 优先保证交互流畅
- 控制节点默认信息密度
- 支持按层级 / 分支懒加载或渐进展示
- 避免一次性渲染过多复杂 DOM

---

## 22. 状态设计规范

### 22.1 必备状态

每个页面 / 组件必须考虑：

- 默认态
- 加载态
- 空状态
- 错误态
- 禁用态
- 成功反馈态
- 权限受限态

### 22.2 加载态

- 列表使用骨架屏或局部 loading
- 按钮提交使用按钮内 loading
- 页面首次加载可使用页面级骨架
- 避免全屏阻塞式 loading

### 22.3 空状态

空状态必须包含：

- 标题
- 说明
- 建议操作
- 主按钮

示例场景：

- 暂无成员
- 暂无标签
- 暂无关系记录
- 暂无搜索结果

### 22.4 错误态

错误态必须区分：

- 网络错误
- 权限错误
- 数据不存在
- 系统异常
- 表单校验错误

### 22.5 权限态

普通用户与管理员看到的操作必须明确区分：

- 无权限操作不可见或禁用
- 禁用时应说明原因
- 不允许点击后才发现无权限

---

## 23. 反馈与提示规范

### 23.1 Toast

用于轻量反馈：

- 保存成功
- 删除成功
- 复制成功
- 操作失败

### 23.2 Inline Message

用于表单或局部模块内反馈。

### 23.3 Confirm Dialog

用于：

- 删除成员
- 删除关系
- 删除标签
- 解绑账号
- 清空数据
- 覆盖导入

### 23.4 文案原则

反馈文案必须：

- 简洁
- 明确
- 可执行
- 不技术化

正确示例：

- 成员信息已保存
- 删除失败，请稍后重试
- 当前账号无权限编辑该成员
- 请先选择数据存储路径

---

## 24. 图标规范

### 24.1 图标库

统一使用 `lucide-react`。

### 24.2 使用原则

- 图标必须辅助理解，不替代文字
- 同一语义使用同一图标
- 图标尺寸统一，常用 `16 / 18 / 20`
- 不允许混用多套图标风格

### 24.3 常见映射建议

- 首页：`Home`
- 族谱树：`TreeDeciduous`
- 成员：`Users`
- 标签：`Tag`
- 关系：`Link2`
- 设置：`Settings`
- 搜索：`Search`
- 通知：`Bell`
- 编辑：`Pencil`
- 删除：`Trash2`

---

## 25. 动效规范

### 25.1 总原则

动效服务于理解，不服务于炫技。

### 25.2 推荐动效

- 淡入淡出
- 轻微上浮
- 缩放进入
- 抽屉滑入
- hover 过渡

### 25.3 时长建议

- 微交互：`120ms ~ 180ms`
- 面板 / 弹窗：`180ms ~ 240ms`
- 页面切换：`200ms ~ 300ms`

### 25.4 禁止事项

- 大幅位移动画
- 高频闪烁
- 复杂连续动画
- 影响阅读的背景动画

---

## 26. 可访问性规范

即使当前以桌面端为主，也必须遵循基础可访问性最佳实践。

### 26.1 基本要求

- 所有交互元素必须可聚焦
- 焦点态必须可见
- 表单必须有关联标签
- 图标按钮必须有可访问名称
- 对比度必须满足可读性要求
- 不依赖颜色作为唯一信息表达方式

### 26.2 键盘支持

关键操作必须支持键盘：

- Tab 导航
- Enter 确认
- Esc 关闭弹窗
- 方向键操作选择器 / 菜单 / 树图局部交互（如适用）

### 26.3 语义化

- 按钮用 `button`
- 导航用 `nav`
- 主内容用 `main`
- 表格用语义表格结构
- 表单控件使用正确语义元素

---

## 27. 国际化与文案规范

### 27.1 当前默认语言

当前产品默认以中文为主。

### 27.2 文案要求

- 使用自然中文
- 避免中英混杂
- 避免开发术语直接暴露给用户
- 同一概念必须统一命名

例如：

- “成员”不要有时写“人物”、有时写“用户”
- “关系标签”不要有时写“标签”、有时写“标记”而无区分

### 27.3 时间与日期

统一格式建议：

- 日期：`YYYY-MM-DD`
- 日期时间：`YYYY-MM-DD HH:mm`
- 生卒信息可简化为：`1901 - 1978`

---

## 28. 权限与角色界面规范

### 28.1 角色区分

至少区分：

- 管理员
- 普通用户

### 28.2 界面策略

- 管理员可见完整管理入口
- 普通用户仅看到与自己相关的可操作内容
- 不可用功能应有明确说明
- 配置关闭的功能不应高调展示

### 28.3 配置控制功能

例如“创建家族”默认关闭时：

- 不应作为主入口展示
- 可在设置或管理员配置中出现
- 若关闭，应显示“当前配置未开启该功能”

---

## 29. 安装与首次启动体验规范

### 29.1 首次启动流程

首次启动必须重点处理：

- 欢迎说明
- 数据存储路径选择
- 本地服务说明
- 隐私与本地存储说明
- 初始化完成反馈

### 29.2 数据路径选择

该流程必须：

- 清晰解释为什么需要路径
- 说明将存储哪些内容
- 提示空间与备份建议
- 支持重新选择
- 支持校验路径可用性

---

## 30. 页面级推荐信息架构

### 30.1 首页

- 家族概览
- 成员统计
- 最近更新
- 快捷操作
- 系统状态

### 30.2 成员管理

- 搜索栏
- 筛选栏
- 成员表格
- 批量操作
- 成员详情侧栏 / 详情页

### 30.3 成员详情

- 头像与基础信息
- 生卒信息
- 关系概览
- 标签
- 生平事迹
- 媒体资料
- 编辑记录

### 30.4 关系管理

- 关系类型筛选
- 关系记录列表
- 新增关系
- 冲突提示
- 关系图辅助查看

### 30.5 标签管理

- 标签列表
- 标签颜色
- 标签分类
- 使用次数
- 新增 / 编辑 / 删除

### 30.6 用户管理

- 账号列表
- 角色权限
- 关联成员
- 登录方式
- 状态管理

### 30.7 设置

- 数据存储路径
- 备份策略
- 本地服务配置
- 局域网访问
- 公网访问 / 内网穿透
- 登录方式配置
- 功能开关

---

## 31. 组件命名与代码实现规范

### 31.1 组件命名

基础组件建议统一放在 `src/components/ui`：

- `Button`
- `Input`
- `Select`
- `Dialog`
- `Tabs`
- `Badge`
- `Card`
- `Table`
- `Avatar`
- `Skeleton`

业务组件建议按领域命名：

- `MemberCard`
- `MemberForm`
- `MemberDetailPanel`
- `RelationEditor`
- `TagManager`
- `TreeToolbar`
- `TreeNodeCard`

### 31.2 组件职责

- 基础组件只解决通用 UI 问题
- 业务组件承载业务语义
- 页面组件负责组合，不承载复杂基础样式逻辑

### 31.3 变体设计

所有基础组件变体必须可预测、可复用、可扩展，不允许页面内临时拼接出“第七种按钮风格”。

---

## 32. 设计评审与代码评审检查清单

每次新增页面或组件时，必须检查：

### 32.1 视觉一致性

- 是否使用统一颜色体系
- 是否使用统一圆角、边框、阴影
- 是否符合排版规范
- 是否符合间距规范

### 32.2 交互一致性

- 主次操作是否清晰
- 状态反馈是否完整
- 弹窗 / 表单 / 表格行为是否一致
- 是否存在误导性点击区域

### 32.3 业务适配性

- 是否符合族谱场景
- 是否支持复杂关系表达
- 是否考虑权限差异
- 是否考虑长文本、长姓名、多标签情况

### 32.4 技术实现质量

- 是否复用基础组件
- 是否使用类型安全 Props
- 是否避免重复样式
- 是否符合 React + TypeScript + Tailwind + Radix 最佳实践

---

## 33. 与 `CLAUDE.md` 的关系

本规范是对 `CLAUDE.md` 中“界面设计”要求的正式补充与落地标准。

### 33.1 强制引用说明

建议在根目录 `CLAUDE.md` 中增加如下说明：

> UI 设计与前端实现必须遵循根目录 `UI_SPEC.md`。所有新增页面、组件、交互与视觉样式，必须符合 UI 规范，不得绕过。

### 33.2 执行要求

后续所有前端开发、设计输出、页面重构、组件封装、视觉调整，均必须以本规范为准。

---

## 34. 后续落地建议

建议下一步按以下顺序推进：

1. 将本规范中的颜色、圆角、阴影、字号、间距整理为统一 design tokens
2. 对 `src/components/ui` 进行标准化收敛
3. 先设计核心页面：
   - 首页
   - 族谱树页
   - 成员列表页
   - 成员详情页
   - 关系管理页
   - 标签管理页
   - 设置页
4. 建立页面模板与复合模式组件
5. 建立 UI Review Checklist，纳入代码评审流程

---

## 35. 最终结论

本项目 UI 必须遵循以下核心原则：

- 统一
- 清晰
- 稳定
- 可维护
- 面向真实族谱业务
- 符合 React + TypeScript + Tailwind CSS + Radix UI 最佳实践

**从现在开始，`genealogy-tree` 的所有前端界面与交互实现，必须符合本 UI 规范。**
